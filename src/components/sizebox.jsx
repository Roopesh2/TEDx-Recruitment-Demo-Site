export const VBox = ({ h }) => (
	<div style={{ minHeight: h + "px", maxHeight: h + "px" }}></div>
);
export const HBox = ({ w }) => (
	<div style={{ minWidth: w + "px", maxWidth: w + "px" }}></div>
);
